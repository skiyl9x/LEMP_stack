properties([pipelineTriggers([githubPush()])])

node {
    stage ('Checkout'){
        git branch: 'main', url: 'https://github.com/skiyl9x/LEMP_stack.git'
    }
    stage ('Build'){
        // steps
    }
    stage ('Test'){
        // steps
    }
}    
